package Accounting.structure;

/*Generated by MPS */

import jetbrains.mps.smodel.PropertySupport;
import java.util.Iterator;
import jetbrains.mps.internal.collections.runtime.ListSequence;

public class Month_PropertySupport extends PropertySupport {
  public boolean canSetValue(String value) {
    if (value == null) {
      return true;
    }
    Iterator<Month> constants = ListSequence.fromList(Month.getConstants()).iterator();
    while (constants.hasNext()) {
      Month constant = constants.next();
      if (value.equals(constant.getName())) {
        return true;
      }
    }
    return false;
  }
  public String toInternalValue(String value) {
    if (value == null) {
      return null;
    }
    Iterator<Month> constants = ListSequence.fromList(Month.getConstants()).iterator();
    while (constants.hasNext()) {
      Month constant = constants.next();
      if (value.equals(constant.getName())) {
        return constant.getValueAsString();
      }
    }
    return null;
  }
  public String fromInternalValue(String value) {
    Month constant = Month.parseValue(value);
    if (constant != null) {
      return constant.getName();
    }
    return "";
  }
}
